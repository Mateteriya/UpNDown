# Архив: экран Обучение (TrainingScreen)

Дата архивации: 2026-07-24

Прототип интерактивного обучения (хаб + 2 урока поверх GameTable) вынесен из продукта.
Заменён экраном «Правила игры» (`#rules` / lab `/rules-lab`).

Исходник сохранён рядом: `TrainingScreen.ARCHIVE.tsx`.
