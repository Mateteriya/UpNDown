# Overlay пример: starkey.agneko.com/updown → 127.0.0.1:3005
# Секреты только в /etc/updown/ws.env (chmod 600), не здесь.

PUBLIC_WS_URL=wss://starkey.agneko.com/updown
# Юнит: deploy/updown-ws.starkey.service
# Caddy: deploy/Caddyfile.path-example
