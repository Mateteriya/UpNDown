# Music (лаб)

Слоты: `menu_bed.wav`, `table_bed.wav` — фоновые петли.

В лабе (`/audio-sfx-lab`) вкладка **Музыка**: бит + атмосфера + фраза → «Сохранить музыку».

`table_bed.wav` также можно пересобрать процедурно: `npm run audio:gen-music` (старый IDM-скрипт).

